{
  "name": "line-notify",
  "version": "0.1.2",
  "description": "Node.js SDK for the LINE Notify API",
  "main": "index.js",
  "scripts": {
    "lint": "tslint --project ./tslint.json --exclude **/node_modules/**/* && echo PASS || echo WARNING",
    "build": "npm run lint && rimraf dist && tsc",
    "install": "npm run build"
  },
  "author": "OUcare",
  "dependencies": {
    "@types/form-data": "0.0.33",
    "@types/node": "^8.10.20",
    "@types/node-fetch": "^1.6.7",
    "form-data": "^2.2.0",
    "node-fetch": "^2.6.1",
    "rimraf": "^2.6.1",
    "tslint": "^5.4.3",
    "typescript": "3.2.4"
  }
}
